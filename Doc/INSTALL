JS8msg V2 RC1

Linux:

Copy the js8msg2 file to a suitable directory, like ~/bin
Set the executable permission like this:  chmod +x js8msg2


Windows 10:

Copy the js8msg2.exe file to your home directory and execute it.


In both cases, js8msg2 will create or update the support directories (and their content files)